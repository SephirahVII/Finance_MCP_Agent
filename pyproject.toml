[project]
name = "invesagent-workspace"
version = "0.1.0"
description = "Workspace metadata for InvesAgent. Install invesagent_mcp and invesagent_agent separately."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.5.0",
]

[tool.setuptools]
packages = []

[tool.ruff]
line-length = 100
target-version = "py311"
